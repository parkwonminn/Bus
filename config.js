/**
 * 배포 설정 — 이 파일만 고치면 됩니다.
 *
 * PROXY_URL     프록시 주소.
 *               Vercel 처럼 페이지와 API 가 같은 도메인이면 '' (빈 문자열) 로 두세요.
 *               프록시가 다른 도메인에 있을 때만 전체 주소를 적습니다. 끝에 / 는 붙이지 마세요.
 *               로컬 개발(정적 8777 + 프록시 8788)은 아래에서 자동으로 처리합니다.
 *
 * KAKAO_JS_KEY  카카오 지도 JavaScript 키.
 *               REST 키와 다른 키입니다. 도메인 제한이 걸리므로 공개되어도 괜찮습니다.
 *               카카오 개발자 콘솔 > 내 애플리케이션 > 플랫폼 > Web 에
 *               배포 주소(https://<프로젝트>.vercel.app)를 반드시 등록하세요.
 */
(function(){
  // 로컬 개발: 정적 서버(8777)로 열었으면 프록시는 8788 에 따로 떠 있다.
  // 그 외(배포): 페이지와 API 가 같은 도메인이므로 상대 경로를 쓴다.
  var localDev = location.port === '8777';

  window.APP_CONFIG = {
    PROXY_URL: localDev ? 'http://127.0.0.1:8788' : '',
    KAKAO_JS_KEY: '098d0417be7c6ea9dbd833fb19b94151'
  };
})();
